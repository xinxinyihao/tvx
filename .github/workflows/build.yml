name: Auto Build APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        abi: [armeabi, arm64]
        brand: [generic, hisense]
        mode: [normal, python]
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Create local.properties
        run: |
          echo "sdk.dir=$ANDROID_HOME" > local.properties
          echo "chaquopy.buildPython=$(which python3)" >> local.properties

      - name: Decode Keystore
        run: |
          echo "${{ secrets.SIGNING_KEY }}" | base64 --decode > release.jks

      - name: Build with Gradle
        # 变体名称格式: [Abi][Brand][Mode]Release
        run: |
          ABI=$(echo "${{ matrix.abi }}" | sed 's/./\U&/')
          BRAND=$(echo "${{ matrix.brand }}" | sed 's/./\U&/')
          MODE=$(echo "${{ matrix.mode == 'normal' && 'normal' || 'python' }}" | sed 's/./\U&/')
          ./gradlew assemble${ABI}${BRAND}${MODE}Release \
            -Pchaquopy.buildPython=$(which python3) \
            -Pandroid.injected.signing.store.file=$(pwd)/release.jks \
            -Pandroid.injected.signing.store.password=${{ secrets.KEY_STORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }} \
            --build-cache --parallel --daemon

      - name: Collect APK
        run: |
          mkdir -p output
          find app/build/outputs/apk/ -name "*Release.apk" -exec cp {} output/ \;
          # 如果文件名中包含 java，将其重命名为更容易识别的名字
          cd output
          for f in *.apk; do mv "$f" "${f//-java/}"; done

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: TVX-${{ matrix.abi }}-${{ matrix.brand }}-${{ matrix.mode }}
          path: output/*.apk

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: all-apks
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: all-apks/*.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
